# 2025년 생성형 AI 오픈소스 생태계 - 스타일 가이드

## 1. 개요

이 문서는 "2025년 생성형 AI 오픈소스 생태계" 웹사이트의 디자인 시스템과 스타일 가이드를 정의합니다. 겨울 테마를 기반으로 한 다크 모드 UI로, 차가우면서도 세련된 느낌을 제공합니다.

---

## 2. 색상 팔레트

### 2.1 Primary Colors (주요 색상)

| 이름 | HEX | RGB | 용도 |
|------|-----|-----|------|
| Primary | `#60a5fa` | rgb(96, 165, 250) | 주요 액션, 링크, 강조 |
| Primary Dark | `#3b82f6` | rgb(59, 130, 246) | 호버 상태, 활성 상태 |
| Primary Light | `#93c5fd` | rgb(147, 197, 253) | 배지, 태그, 서브텍스트 |

### 2.2 Secondary Colors (보조 색상)

| 이름 | HEX | RGB | 용도 |
|------|-----|-----|------|
| Secondary (Cyan) | `#67e8f9` | rgb(103, 232, 249) | 성공 상태, 하이라이트 |
| Accent (Lavender) | `#c4b5fd` | rgb(196, 181, 253) | 액센트, 보조 강조 |

### 2.3 Background Colors (배경 색상)

| 이름 | HEX | RGB | 용도 |
|------|-----|-----|------|
| Dark | `#0c1929` | rgb(12, 25, 41) | 메인 배경 |
| Dark Light | `#162033` | rgb(22, 32, 51) | 섹션 배경, 카드 배경 |
| Gray 900 | `#0f1a2a` | rgb(15, 26, 42) | 깊은 배경 |
| Gray 800 | `#1a2744` | rgb(26, 39, 68) | 입력 필드 배경 |

### 2.4 Text Colors (텍스트 색상)

| 이름 | HEX | 용도 |
|------|-----|------|
| White | `#ffffff` | 제목, 강조 텍스트 |
| Gray 300 | `#d1e3f6` | 본문 텍스트 |
| Gray 400 | `#a8c5e8` | 부제목, 설명 텍스트 |
| Gray 500 | `#7b9bc2` | 비활성 텍스트, 플레이스홀더 |
| Gray 600 | `#4a6085` | 보조 텍스트 |

### 2.5 Special Effects (특수 효과)

| 이름 | 값 | 용도 |
|------|-----|------|
| Frost | `rgba(255, 255, 255, 0.08)` | 유리/서리 효과 |
| Ice | `rgba(147, 197, 253, 0.15)` | 얼음 효과 |
| Snow | `rgba(255, 255, 255, 0.95)` | 눈 효과 |

---

## 3. 그라디언트

### 3.1 Primary Gradient
```css
background: linear-gradient(135deg, #60a5fa 0%, #818cf8 50%, #c4b5fd 100%);
```
용도: 버튼, 프로그레스 바, 강조 요소

### 3.2 Dark Gradient
```css
background: linear-gradient(180deg, #0c1929 0%, #162033 50%, #1a2744 100%);
```
용도: 페이지 배경

### 3.3 Card Gradient
```css
background: linear-gradient(145deg, rgba(255,255,255,0.08) 0%, rgba(255,255,255,0.03) 100%);
```
용도: 카드, 모달 배경

### 3.4 Frost Gradient
```css
background: linear-gradient(135deg, rgba(147, 197, 253, 0.2) 0%, rgba(196, 181, 253, 0.1) 100%);
```
용도: 하이라이트 카드, 선택된 요소

---

## 4. 타이포그래피

### 4.1 폰트 패밀리
```css
font-family: 'Noto Sans KR', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

### 4.2 폰트 크기

| 요소 | 크기 | 굵기 | 행간 |
|------|------|------|------|
| Hero Title | `clamp(2.5rem, 8vw, 4.5rem)` | 700 | 1.1 |
| Section Title | `clamp(2rem, 5vw, 3rem)` | 700 | 1.2 |
| Card Title | `1.5rem` | 700 | 1.3 |
| Subtitle | `1.25rem` | 500 | 1.5 |
| Body | `1rem` | 400 | 1.7 |
| Small | `0.875rem` | 400 | 1.5 |
| Caption | `0.8rem` | 500 | 1.4 |

### 4.3 폰트 굵기

| 이름 | 값 | 용도 |
|------|-----|------|
| Light | 300 | 대형 텍스트 |
| Regular | 400 | 본문 |
| Medium | 500 | 강조 본문 |
| Semibold | 600 | 소제목 |
| Bold | 700 | 제목 |

---

## 5. 간격 시스템

### 5.1 기본 단위
- 기본 단위: `4px`
- 스케일: 4, 8, 12, 16, 20, 24, 32, 40, 48, 60, 80, 120

### 5.2 적용 예시

| 용도 | 값 |
|------|-----|
| 카드 내부 패딩 | `32px` (모바일: `24px`) |
| 섹션 패딩 | `120px 0` (모바일: `80px 0`) |
| 요소 간 간격 | `24px` |
| 컴포넌트 내부 간격 | `16px` |
| 텍스트 간격 | `8px - 12px` |

---

## 6. 테두리 & 모서리

### 6.1 Border Radius

| 이름 | 값 | 용도 |
|------|-----|------|
| Small | `6px` | 버튼, 태그, 배지 |
| Default | `12px` | 카드, 입력 필드 |
| Large | `20px` | 모달, 큰 카드 |
| Full | `50px` | 원형 배지, 토글 |

### 6.2 Border Style
```css
border: 1px solid rgba(147, 197, 253, 0.1);  /* 기본 */
border: 1px solid rgba(147, 197, 253, 0.3);  /* 호버 */
border: 1px solid rgba(147, 197, 253, 0.4);  /* 활성 */
```

---

## 7. 그림자 & 효과

### 7.1 Box Shadow

| 이름 | 값 | 용도 |
|------|-----|------|
| Small | `0 1px 2px 0 rgba(0, 0, 0, 0.1)` | 미묘한 깊이 |
| Default | `0 4px 6px -1px rgba(0, 0, 0, 0.15)` | 카드, 버튼 |
| Large | `0 10px 15px -3px rgba(0, 0, 0, 0.15)` | 모달, 드롭다운 |
| XL | `0 20px 25px -5px rgba(0, 0, 0, 0.15)` | 오버레이 |
| Glow | `0 0 40px rgba(96, 165, 250, 0.4), 0 0 80px rgba(147, 197, 253, 0.2)` | 강조 버튼 |
| Frost | `0 8px 32px rgba(147, 197, 253, 0.15)` | 겨울 테마 카드 |

### 7.2 Backdrop Filter
```css
backdrop-filter: blur(10px);  /* 카드 */
backdrop-filter: blur(20px);  /* 네비게이션 */
backdrop-filter: blur(5px);   /* 배지 */
```

---

## 8. 애니메이션

### 8.1 Transition
```css
transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
```

### 8.2 Keyframe Animations

#### Fade In Up
```css
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
```

#### Snowfall
```css
@keyframes snowfallAnim {
    0% {
        transform: translateY(0) translateX(0) rotate(0deg);
        opacity: 1;
    }
    100% {
        transform: translateY(105vh) translateX(0) rotate(360deg);
        opacity: 0.3;
    }
}
```

#### Pulse
```css
@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
}
```

### 8.3 Animation Timing
- 빠른 전환: `0.15s`
- 기본 전환: `0.3s`
- 느린 전환: `0.6s`
- 배경 애니메이션: `10s - 30s`

---

## 9. 컴포넌트 스타일

### 9.1 버튼

#### Primary Button
```css
.btn-primary {
    padding: 14px 32px;
    background: linear-gradient(135deg, #60a5fa 0%, #818cf8 50%, #c4b5fd 100%);
    color: #ffffff;
    border-radius: 10px;
    font-weight: 600;
    box-shadow: 0 0 40px rgba(96, 165, 250, 0.4);
}
```

#### Secondary Button
```css
.btn-secondary {
    padding: 14px 32px;
    background: rgba(255, 255, 255, 0.05);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 10px;
}
```

### 9.2 카드

```css
.card {
    background: linear-gradient(145deg, rgba(255,255,255,0.08) 0%, rgba(255,255,255,0.03) 100%);
    border: 1px solid rgba(147, 197, 253, 0.1);
    border-radius: 20px;
    padding: 32px;
    backdrop-filter: blur(10px);
}

.card:hover {
    border-color: rgba(147, 197, 253, 0.4);
    transform: translateY(-8px);
    box-shadow: 0 8px 32px rgba(147, 197, 253, 0.15);
}
```

### 9.3 배지/태그

```css
.badge {
    padding: 6px 16px;
    background: rgba(147, 197, 253, 0.1);
    border: 1px solid rgba(147, 197, 253, 0.2);
    border-radius: 50px;
    font-size: 0.8rem;
    font-weight: 600;
    color: #93c5fd;
    text-transform: uppercase;
    letter-spacing: 1px;
}
```

### 9.4 네비게이션

```css
.navbar {
    background: rgba(12, 25, 41, 0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(147, 197, 253, 0.1);
}

.nav-link {
    padding: 10px 18px;
    border-radius: 8px;
    color: #a8c5e8;
}

.nav-link:hover,
.nav-link.active {
    color: #93c5fd;
    background: rgba(147, 197, 253, 0.1);
}
```

---

## 10. 반응형 브레이크포인트

| 이름 | 값 | 대상 |
|------|-----|------|
| Mobile | `max-width: 480px` | 스마트폰 |
| Tablet | `max-width: 768px` | 태블릿, 작은 노트북 |
| Desktop | `min-width: 769px` | 데스크탑 |

### 10.1 반응형 조정

#### 모바일 (480px 이하)
- Hero 타이틀: `2rem`
- 섹션 패딩: `80px 0`
- 카드 패딩: `24px`
- 버튼 너비: `100%`

#### 태블릿 (768px 이하)
- 네비게이션: 햄버거 메뉴로 전환
- 그리드: 1열 레이아웃
- 화살표 방향: 90도 회전

---

## 11. 접근성

### 11.1 컬러 대비
- 본문 텍스트: 최소 4.5:1 대비율
- 대형 텍스트: 최소 3:1 대비율
- 인터랙티브 요소: 명확한 포커스 상태

### 11.2 인터랙션
- 모든 클릭 가능 요소: 최소 44x44px 터치 영역
- 호버/포커스 상태 명확히 구분
- 키보드 네비게이션 지원

---

## 12. 코드 블록 스타일

### 12.1 구문 강조 색상

| 요소 | 색상 |
|------|------|
| 키워드 | `#c4b5fd` |
| 클래스명 | `#67e8f9` |
| 함수명 | `#93c5fd` |
| 문자열 | `#a5f3fc` |
| 주석 | `#7b9bc2` |
| 기본 텍스트 | `#d1e3f6` |

### 12.2 코드 블록 스타일
```css
.code-block {
    background: rgba(12, 25, 41, 0.9);
    border: 1px solid rgba(147, 197, 253, 0.15);
    border-radius: 20px;
    font-family: 'JetBrains Mono', 'Fira Code', monospace;
    font-size: 0.85rem;
    line-height: 1.6;
}
```

---

## 13. 아이콘 & 이모지

### 13.1 사용 가이드
- 시스템 아이콘 대신 이모지 사용
- 각 섹션별 대표 이모지로 시각적 구분
- 크기: 섹션 아이콘 `3rem`, 인라인 `1.5rem`

### 13.2 주요 이모지
- 로봇: 🤖
- 차트: 📊
- 설정: ⚙️
- 게임: 🎮
- 번개: ⚡
- 타겟: 🎯
- 문서: 📝
- 방패: 🛡️

---

## 14. 파일 구조

```
/
├── index.html          # 메인 HTML
├── styles.css          # 스타일시트
├── script.js           # JavaScript
├── STYLE_GUIDE.md      # 이 문서
└── source.txt          # 원본 콘텐츠
```

---

## 15. 버전 정보

- **버전**: 1.0.0
- **생성일**: 2025년 12월
- **테마**: Winter / Frost
- **디자인 컨셉**: 차가운 겨울밤, 서리와 눈
